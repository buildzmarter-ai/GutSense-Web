# GutSense Frontend Environment Variables
# Set NEXT_PUBLIC_API_URL to your deployed backend URL (no trailing slash).
# Locally it defaults to http://localhost:8000
# For production, copy this to .env.production and set the Railway URL.

NEXT_PUBLIC_API_URL=https://your-gutsense-api.up.railway.app
